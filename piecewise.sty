\ProvidesPackage{piecewise}

\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{physics}
\usepackage{import}
\usepackage{hyperref}
\usepackage[framemethod=TikZ]{mdframed}

% yay for proofs
\usepackage{amsthm}
% fixing up typographic things
\usepackage{microtype}
% really conflicted on this: i like indentation on paragraphs,
% but having no space between them makes the content *really* dense.
\usepackage{parskip}

% doc info
\title{Piecewise Notes}
\author{Ally Macdonald}
\date{}

% theorems and things
\theoremstyle{definition}
\newmdtheoremenv[%
    outerlinecolor = black, %
    splittopskip=2\baselineskip,
    linewidth = 0.5pt, %
    roundcorner = 2pt, %
    everyline=true, % frame split across pages
    middleextra={\node[anchor=north west,inner xsep=0pt,xshift=10pt] at (P-|O) {\bfseries{\thetheorem}\ \mdseries{(continued)}\bfseries{.}};},
    secondextra={\node[anchor=north west,inner xsep=0pt,xshift=10pt] at (P-|O) {\bfseries{\thetheorem}\ \mdseries{(continued)}\bfseries{.}};}
]{theorem}{}[subsection]

\theoremstyle{definition}
\newmdtheoremenv[%
    outerlinecolor = black, %
    linewidth = 0.5pt, %
    roundcorner = 2pt, %
    splittopskip=2\baselineskip,
    everyline=true, % frame split across pages
    middleextra={\node[anchor=north west,inner xsep=0pt,xshift=10pt] at (P-|O) {\bfseries{Example~\theexample}\ \mdseries{(continued)}\bfseries{.}};},
    secondextra={\node[anchor=north west,inner xsep=0pt,xshift=10pt] at (P-|O) {\bfseries{Example~\theexample}\ \mdseries{(continued)}\bfseries{.}};}
]{example}{Example}[subsection]

% my first environment
% i hate amsmath doesn't support multicase piecewise
\newenvironment{piecewise}[1]
    {
        \renewcommand*{\arraystretch}{1.2}
            \!\left\{
                \begin{matrix*}[l]
                #1
    }
    {
                \end{matrix*}
            \right.
    }

% fun commands
\newcommand{\dom}[1]{\operatorname{dom}\paren*{#1}}
\newcommand{\pwobj}[3]{\left\{#1,\quad #2\ \middle|\ #3\right\}} % hack bc lazy
\newcommand{\pwarg}[2]{\br*{#1,\quad #2}}

\newcommand{\sgn}[1]{\mathrm{\mathop{sgn}}\paren*{#1}}
\newcommand{\step}[1]{\mathrm{\mathop{H}}\paren*{#1}}


% delim
\DeclarePairedDelimiter{\paren}{(}{)}
\DeclarePairedDelimiter{\br}{\{}{\}}
\DeclarePairedDelimiter{\brak}{[}{]}
\DeclarePairedDelimiter{\brakp}{[}{)}
\DeclarePairedDelimiter{\pbrak}{()}{]}
\DeclarePairedDelimiter{\floor}{\lfloor}{\rfloor}
\DeclarePairedDelimiter{\ceil}{\lceil}{\rceil}